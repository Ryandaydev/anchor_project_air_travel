[project]
name = "air-travel-cli"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "typer>=0.12.0",
    "httpx>=0.27.0",
]

[project.scripts]
air-travel = "air_travel_cli.main:main_entry"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"